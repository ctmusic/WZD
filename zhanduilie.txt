栈 仅在表尾进行插入和删除操作的线性表，表尾（栈顶）表头（栈底）
后进先出，退栈的第一个元素为栈顶元素
顺序栈：连续存储的线性表
链栈：运算受限的单链表，插入和操作在表头进行，栈顶指针为链表头指针
应用：数制转换，表达式处理，括号匹配，函数调用，递归问题

队列 只能在一端进行插入一端进行删除的线性表，队尾（允许插入的一端），队头（允许删除的一端）
先进先出
队列的链表表示为链队列，限制在表头删除表尾插入的单链表，尾指针指向链表最后一个结点
应用：模拟事件，操作系统资源管理

