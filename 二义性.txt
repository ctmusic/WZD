二义性指函数重名导致的函数无法正确调用的问题，具体可能出现的问题有：
    1.函数重载时会有函数重名问题，但此时可以用不同的参数列表来解决二义性问题
	2.函数的继承与派生时如果出现菱形问题时会有对基类的数据调用会报错，此时对派生类采用虚继承就行
	3.应当考虑避免而不是解决的二义性问题：
	      1.函数重载时只有返回值不同会导致二义性
		  2.函数重载时参数列表只把参数改为引用类型会导致二义性